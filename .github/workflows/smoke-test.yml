name: Smoke Test

"on":
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  smoke:
    name: Smoke
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow starts
        run: echo "GitHub Actions is working"
